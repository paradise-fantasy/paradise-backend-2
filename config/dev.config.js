const config = {};

config.postgres = {
  host: 'localhost',
  database: 'paradise',
  username: 'postgres',
  password: 'paradise'
}

config.mongodb = {
  uri: 'mongodb://localhost/paradise'
}

export default config;
